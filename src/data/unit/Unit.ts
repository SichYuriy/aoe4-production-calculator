export default class Unit {
    public id: string;
    public name: string;
    public icon: any;

    constructor(id: string, name: string, icon: any) {
        this.id = id;
        this.name = name;
        this.icon = icon;
    }
}
